let x = num1;
let y = num2;
let (x + y) = add;
let (x - y) = minus;
let (x * y) = multiply;
let (x / y) = divide;

if (num1 + num2);

const number = prompt("Please input some number.");
  if (!number) {
    pleaseTryAgain();
  } else {
    myHeading.textContent = `answer, ${number}`;
  }

